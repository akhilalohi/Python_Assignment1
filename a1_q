lst = input("Enter the list of numbers separated by space: ").split()
lst = [int(x) for x in lst]  # Convert each input to an integer
minimum = min(lst)
maximum = max(lst)
print("The minimum value is ",minimum," and the maximum value is" ,maximum)
